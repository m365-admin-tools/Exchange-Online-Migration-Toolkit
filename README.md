# Exchange-Online-Migration-Toolkit
Interactive menu for the repetitive per-user tasks in an Exchange Online migration: stamping the on-premises AD attributes that route mail to the cloud mailbox, scheduling move request completion, updating UPNs to the vanity domain, checking licence assignment, and checking move status. Each task runs against a single account or list of users.
